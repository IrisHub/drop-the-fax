import { WebShareConfig } from './types';
export declare const share: (config: WebShareConfig) => () => any;
declare const paymentRequest: <TProps extends object>() => (WrappedComponent: any) => any;
export default paymentRequest;
